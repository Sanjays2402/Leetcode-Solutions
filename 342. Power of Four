class Solution {
public:
    bool isPowerOfFour(int num) {
        
        if (num <= 0)
		    return false;
        
	    double value = log10(num) / log10(4);
	    return ceil(value) == floor(value);
        
    }
};
