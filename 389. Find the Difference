class Solution {
public:
    char findTheDifference(string s, string t) {
        
        char a=0;
        for(char i: s)
            a ^= i;
        for(char i: t)
            a ^= i;
        
        return a;
    }
};
